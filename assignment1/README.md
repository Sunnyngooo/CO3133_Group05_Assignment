### Hardware configuration

- **Platform:** Windows 64-bit, x64-based architecture
- **Processor (CPU):** Intel(R) Core(TM) i7-8665U CPU @ 1.90GHz (Max 2.11 GHz, 4 cores, 8 threads)
- **System Memory (RAM):** 16.0 GB (15.7 GB usable)
- **GPU Accelerator:** None / Integrated Intel(R) UHD Graphics 620
- **Execution Target:** CPU execution using PyTorch CPU build (`2.14.0+cpu`)
  Device name LAPTOP-39UGNPPP
  Processor Intel(R) Core(TM) i7-8665U CPU @ 1.90GHz (2.11 GHz)
  Installed RAM 16.0 GB (15.7 GB usable)
  Graphics card Intel(R) UHD Graphics 620 (128 MB)
  Storage 290 GB of 954 GB used
  Device ID FD1E4B7C-F041-400C-B33F-3814DE4247DB
  Product ID 00330-52678-57027-AAOEM
  System type 64-bit operating system, x64-based processor
  Pen and touch Pen and touch support with 10 touch points

### Dependency Versions & Setup

- **Python Version:** 3.12.10
- Setup the environment using `requirements.txt`:

  ```bash
  pip install -r requirements.txt
  ```

### Environment Setup

Follow these steps to set up the project on Windows:

1. Open a terminal in the project root:

   ```bash
   cd "c:\your_root\CO3133_Group05_Assignment"
   ```

2. Create a virtual environment:
   ```bash
    python -m venv venv
   ```
3. Activate the virtual environment:
   ```bash
   .\venv\Scripts\Activate.ps1
   ```
4. Install the requirements:
   ```bash
   pip install -r assignment1\requirements.txt
   ```
   Then open the `draft.ipynb` and select the created venv for kernel.
